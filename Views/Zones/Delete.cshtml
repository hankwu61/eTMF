@model ETMF.Models.Zone

@{
    ViewData["Title"] = "删除区域";
}

<div class="card shadow mb-4">
    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
        <h6 class="m-0 font-weight-bold text-danger">
            <i class="fas fa-trash"></i> 删除区域
        </h6>
        <div class="dropdown no-arrow">
            <a class="btn btn-secondary btn-sm" asp-action="Index">
                <i class="fas fa-arrow-left"></i> 返回列表
            </a>
        </div>
    </div>
    <div class="card-body">
        <div class="alert alert-danger">
            <h5><i class="fas fa-exclamation-triangle"></i> 警告</h5>
            <p>您确定要删除这个区域吗？此操作将同时删除该区域下的所有部分、文档类型和文档，且无法恢复！</p>
        </div>
        
        <div class="row">
            <div class="col-md-6">
                <dl class="row">
                    <dt class="col-sm-4">编号</dt>
                    <dd class="col-sm-8">@Model.Number</dd>
                    
                    <dt class="col-sm-4">名称</dt>
                    <dd class="col-sm-8">@Model.Name</dd>
                    
                    <dt class="col-sm-4">描述</dt>
                    <dd class="col-sm-8">@Model.Description</dd>
                    
                    <dt class="col-sm-4">包含部分数量</dt>
                    <dd class="col-sm-8">@(Model.Sections?.Count ?? 0)</dd>
                    
                    <dt class="col-sm-4">创建时间</dt>
                    <dd class="col-sm-8">@Model.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")</dd>
                </dl>
            </div>
        </div>
        
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">
                <i class="fas fa-trash"></i> 确认删除
            </button>
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fas fa-times"></i> 取消
            </a>
        </form>
    </div>
</div> 